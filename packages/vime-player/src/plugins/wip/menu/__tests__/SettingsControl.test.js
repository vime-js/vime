describe('components', () => {
  describe('control', () => {
    describe('SettingsControl', () => {
      // settings control rendered correct, pressing it shows right stuff icon.
      // on click shows menu.
      // check tooltips.
      // accessiblity
      // dig through menu and do the same.
      it('', () => {
      })
    })
  })
})
