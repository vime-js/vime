describe('components', () => {
  describe('control', () => {
    describe('VolumeControl', () => {
      // volume control rendered correct, volume states (mute, low, high), changing volume
      // calls provider.setVolume, muting calls provider and makes change.
      // test keyboard.
      // check tooltips.
      // accessiblity
      it('', () => {
      });
    });
  });
});
