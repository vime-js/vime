describe('components', () => {
  describe('control', () => {
    describe('SettingsControl', () => {
      // seek control rendered correct, shows right icon (forward/backward)
      // pressing it adds correctly and calls provider.setCurrentTime (forward/backward)
      // pressing it shows correct totalDisplay (forward/backward)
      // accessiblity
      it('', () => {
      })
    })
  })
})
