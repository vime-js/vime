describe('components', () => {
  describe('control', () => {
    describe('ScrubberControl', () => {
      // scrubber control rendered correct, changing calls provider.setCurrentTime,
      // is buffered loading (progress bar)?
      // accessiblity
      // tooltips
      it('', () => {
      })
    })
  })
})
