describe('components', () => {
  describe('control', () => {
    describe('PlaybackControl', () => {
      // playback control rendered correct, pressing play shows right stuff (icon etc) and
      // provider.setPaused(false) // is called.
      // test keyboard.
      // check tooltips.
      // click to play.
      // accessiblity
      it('', () => {
      })
    })
  })
})
