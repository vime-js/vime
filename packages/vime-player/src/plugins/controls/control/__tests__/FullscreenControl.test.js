describe('components', () => {
  describe('control', () => {
    describe('FullscreenControl', () => {
      // fullscreen control rendered correct, pressing it shows right stuff icon.
      // esc works (shows right icon).
      // screenfull is called or document whatever.
      // test keyboard.
      // check tooltips.
      // accessiblity
      it('', () => {
      })
    })
  })
})
