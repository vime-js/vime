describe('components', () => {
  describe('control', () => {
    describe('PiPControl', () => {
      // pip control rendered correct, pressing it shows right stuff icon.
      // provider.setPiP // is called.
      // test keyboard.
      // check tooltips.
      // accessiblity
      it('', () => {
      })
    })
  })
})
