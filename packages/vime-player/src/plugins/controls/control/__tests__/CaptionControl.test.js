describe('components', () => {
  describe('control', () => {
    describe('CaptionControl', () => {
      // caption control rendered correct, pressing it shows right stuff icon etc.
      // isCaptionsEnabled changes (context) // is called.
      // test keyboard.
      // accessiblity
      // check tooltips.
      it('', () => {
      })
    })
  })
})
