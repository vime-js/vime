describe('components', () => {
  describe('Controls', () => {
    it('should be structurally correct', () => {
      // const { getByTestId } = render(Player)
      // expect(getByTestId('controls')).toMatchSnapshot()
    });

    // is it rendered correctly if is live stream?
    // video -> desktop/desktopLive, mobile/mobileLive
    // audio -> desktop/desktopLive, mobile/mobileLive
    it('', () => {
    });

    // idle / not
    it('', () => {
    });

    describe('audio', () => {
      // is it rendered correctly?
      it('', () => {
      });

      // is it rendered correctly if is live stream?
      it('', () => {
      });

      // is it rendered correctly if mobile?
      it('', () => {
      });
    });

    describe('mobile', () => {
      // are bottom controls rendered correct?
      it('', () => {
      });

      // is mobile controls rendered correct?
      it('', () => {
      });

      // is mobile controls rendered correct if live stream?
      it('', () => {
      });

      // idle / not
      it('', () => {
      });
    });
  });
});
