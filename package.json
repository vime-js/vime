{
  "name": "vime",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "build": "node .scripts/build.js",
    "release:prepare": "node .scripts/prepare.js",
    "release:prepare.ci": "node .scripts/prepare.js --ci",
    "release": "node .scripts/release.js",
    "validate": "npm run release:prepare && node .scripts/validate.js",
    "changelog": "conventional-changelog -p angular -i ./CHANGELOG.md -k core -s"
  },
  "devDependencies": {
    "@octokit/rest": "^18.0.12",
    "conventional-changelog-cli": "^2.1.1",
    "conventional-recommended-bump": "^6.0.11",
    "execa": "^5.0.0",
    "fs-extra": "^9.0.1",
    "kleur": "^4.1.3",
    "listr": "^0.14.3",
    "rimraf": "^3.0.2",
    "semver": "^6.3.0"
  }
}
